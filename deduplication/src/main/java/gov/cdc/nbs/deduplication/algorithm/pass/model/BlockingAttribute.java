package gov.cdc.nbs.deduplication.algorithm.pass.model;

public enum BlockingAttribute {
    FIRST_NAME,
    LAST_NAME,
    BIRTHDATE,
    SEX,
    ADDRESS,
    ZIP,
    EMAIL,
    PHONE,
    IDENTIFIER
}
