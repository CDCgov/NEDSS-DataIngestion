apiVersion: v1
kind: Service
metadata:
  name: service-kafka
spec:
  ports:
  - port: 9092
    name: external
    protocol: TCP
  selector:
    service: kafka
  # used LoadBalancer for testing
  type: LoadBalancer